Investigate and fix the CI failure described in the arguments below.

Steps:
1. Read the error logs to understand what failed and why
2. Locate the relevant source files in the repository
3. Fix the root cause of the failure
4. Stage the changes, commit them to the branch specified in "Branch Name", and push it
5. If the error logs include post-fix deployment steps (e.g. updating release tags, publishing artifacts), execute them

$ARGUMENTS
